name: 🚀 Deploy automático para o GitHub Pages (main → gh-pages)

on:
  push:
    branches:
      - main   # publica sempre que houver atualização no main
  workflow_dispatch:  # permite acionar manualmente também

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout código do main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🏗️ Preparar build estático
        run: |
          echo "🔧 Copiando arquivos do main para pasta de publicação..."
          mkdir public
          cp -r * public/
          echo "✅ Build pronto!"

      - name: 📦 Upload dos arquivos
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

      - name: 🌐 Publicar no branch gh-pages
        uses: actions/deploy-pages@v4
