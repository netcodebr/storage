name: 🚀 Deploy manual para GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🏗️ Build estático
        run: echo "Nenhum build adicional necessário para este projeto."

      - name: 🚀 Publicar no GitHub Pages
        uses: actions/configure-pages@v5

      - name: 🔧 Upload dos arquivos
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./

      - name: 🌐 Deploy final
        uses: actions/deploy-pages@v4
